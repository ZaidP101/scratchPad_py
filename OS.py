# 'python docs os' : This will help you to carry out multiple tasks using python in operating System

import os
import shutil
print(os.getcwd())
# current working Directory
print(shutil.disk_usage("/"))
# sh = shell (any work related to shell 'shutil' is used) "/" = root (entire system)
# pip install xyz (any library) = pip is python package installer
# pip install boto3 = boto3 is a library the allows you to acess your AWS using python
